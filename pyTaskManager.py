#Initial Commit
#test

import os
import sys
print("test")

#check that module(s) were imported
#check for multiple modules using a list
# modulenames = ["os"]
# for x in modulenames:
#     if modulenames[x] not in sys.modules:
#         print(modulenames[x], " is not imported.")

#check for one module at a time